# Filament Surface

The security Filament plugin currently owns one operator page:

- `OpsSecurityPage`

Keep that page aligned with the declared ops module `ops-security` and the current security posture summary surface.
