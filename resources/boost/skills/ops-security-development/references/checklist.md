# Ops Security Checklist

- confirm feature, ops-module, request, and requirement declarations still match the real package surface
- confirm sensitive preview fields remain sanitized and masked correctly
- confirm install and doctor declarations still match package bindings
- confirm package tests cover the changed resolver, broker, page, or action path
